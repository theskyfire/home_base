##############################################################################
# Project Template
##############################################################################
IncludingMakefile:=$(IncludingMakefile)
include $(guard)
IncludingMakefile=$(value OrigIncludingMakefile)
##############################################################################

# prg: install(prj)
$(this): install($(this))

$(foreach stage,$(stages),\
	$(eval $(stage)_$(this)=$(tmp_PATH)/$(this).$(stage).log)\
)

##############################################################################
# vim: set syntax=make:
